import { Input } from "@nextui-org/react";

const Search = () => {
  return (
    <Input
      clearable
      bordered
      fullWidth
      color="primary"
      size="lg"
      placeholder="Search"
    />
  );
};

export default Search;
